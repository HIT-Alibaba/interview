{
  "name": "interview",
  "version": "0.0.0",
  "scripts": {
    "pretest": "gitbook install source",
    "test": "gitbook build source"
  },
  "devDependencies": {
    "gitbook-cli": "^1.0.0"
  }
}
